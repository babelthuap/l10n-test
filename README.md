Install devDependencies with `npm i`.

Run `gulp prepare` to regenerate `locales/en.json`.

Run a server from `locales/` in order to edit `ja.json`.

Finally, run `gulp` to generate a build in `build/`.
